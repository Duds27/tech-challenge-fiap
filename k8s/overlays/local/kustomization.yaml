apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Overlay LOCAL / kind (desenvolvimento): MySQL in-cluster (StatefulSet) e
# ConfigMap/Secret de exemplo. Aplique com: kubectl apply -k k8s/overlays/local
namespace: oficina

resources:
  - ../../base
  - 10-configmap.yaml
  - 11-secret.yaml
  - 20-mysql.yaml
